POSITION: this is the FIRST session of the course.

There is no previous session, so Rule 1 cannot lean on one. Instead:

- Build the hook from the HOOK FOUNDATION section supplied below. That is the
  author's statement of what the learner arrives with. Do not invent a different
  starting point, and do not assume any session of THIS course was taught.
- The Recap table recaps the PREREQUISITES the learner brings, not a previous
  session. Title it so this is obvious, e.g. "What you already bring". State in
  one line where that prior knowledge comes from.
- If no HOOK FOUNDATION was supplied, write the hook section with
  `[NEEDS: what the learner arrives knowing]` rather than guessing.
- What's Next points forward to the session that now follows this one.
